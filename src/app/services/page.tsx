import ServicesCard from "@/components/Services";
import React from "react";

const page = () => {
  return (
    <section className="lg:ml-[360px]">
      <ServicesCard />
    </section>
  );
};

export default page;
